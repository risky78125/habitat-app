<wxs module="tx" src="./tx-types.wxs" />

<view class="page">
  <navigation-bar title="星力明细" />

  <view class="filter-bar">
    <view class="filter-item {{filter === 'all' ? 'active' : ''}}" data-filter="all" bindtap="onFilterChange">
      <text class="filter-text">全部</text>
    </view>
    <view class="filter-item {{filter === 'income' ? 'active' : ''}}" data-filter="income" bindtap="onFilterChange">
      <text class="filter-text">收入</text>
    </view>
    <view class="filter-item {{filter === 'expense' ? 'active' : ''}}" data-filter="expense" bindtap="onFilterChange">
      <text class="filter-text">支出</text>
    </view>
  </view>

  <scroll-view type="list" scroll-y class="list-area" enhanced show-scrollbar="{{false}}">
    <loading-dots show="{{loading}}" />

    <block wx:if="{{!loading}}">
      <view wx:if="{{filteredTransactions.length === 0}}" class="empty">
        <text class="empty-icon">📊</text>
        <text class="empty-text">暂无{{filter === 'income' ? '收入' : filter === 'expense' ? '支出' : ''}}记录</text>
      </view>

      <view wx:else class="list">
        <view class="tx-item" wx:for="{{filteredTransactions}}" wx:key="id">
          <view class="tx-icon-wrap tx-{{item.type}}">
            <image class="tx-icon" src="{{tx.icon(item.type)}}" mode="aspectFit" />
          </view>
          <view class="tx-body">
            <text class="tx-type">{{tx.label(item.type)}}</text>
            <text class="tx-remark" wx:if="{{item.remark}}">{{item.remark}}</text>
            <text class="tx-time">{{item.displayTime}}</text>
          </view>
          <view class="tx-right">
            <text class="tx-amount {{item.amount > 0 ? 'income' : 'expense'}}">{{item.amount > 0 ? '+' : ''}}{{item.amount}}</text>
            <text class="tx-balance">余额 {{item.balanceAfter}}</text>
          </view>
        </view>

        <view wx:if="{{loadingMore}}" class="loading-more">
          <text class="loading-more-text">加载中...</text>
        </view>
        <view wx:if="{{!hasMore && filteredTransactions.length > 0}}" class="no-more">
          <text class="no-more-text">没有更多了</text>
        </view>

        <view class="bottom-spacer"></view>
      </view>
    </block>
  </scroll-view>
</view>
